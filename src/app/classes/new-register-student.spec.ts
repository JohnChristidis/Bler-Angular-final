import { NewRegisterStudent } from './new-register-student';

describe('NewRegisterStudent', () => {
  it('should create an instance', () => {
    expect(new NewRegisterStudent()).toBeTruthy();
  });
});
